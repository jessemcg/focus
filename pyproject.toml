[project]
name = "focus"
version = "0.1.0"
description = "Libadwaita GTK4 app for browsing court transcripts with paging, grep, images, and AI summaries/RAG."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chromadb>=1.4.1",
    "langchain>=1.2.6",
    "langchain-chroma>=1.1.0",
    "langchain-voyageai>=0.3.2",
    "pygobject==3.54.5",
]
